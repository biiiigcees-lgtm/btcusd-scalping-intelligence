{
  "name": "@btc/db",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "studio": "drizzle-kit studio",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'ok' && exit 0",
    "test": "echo 'db schema tests pending' && exit 0",
    "build": "echo 'ok' && exit 0"
  },
  "dependencies": {
    "drizzle-orm": "^0.33.0",
    "postgres": "^3.4.4",
    "@btc/shared": "workspace:*"
  },
  "devDependencies": {
    "drizzle-kit": "^0.24.2",
    "typescript": "^5.6.3"
  }
}
