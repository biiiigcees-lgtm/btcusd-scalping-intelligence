{
  "name": "@btc/ml",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "echo 'ml baseline has no unit tests yet' && exit 0",
    "lint": "echo 'ok' && exit 0",
    "build": "echo 'ok' && exit 0"
  },
  "dependencies": {
    "@btc/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
